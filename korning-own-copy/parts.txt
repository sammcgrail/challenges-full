employee x
customer_and_account_no x
product_name x
sale_date x
sale_amount
units_sold
invoice_no
invoice_frequency

CREAT TABLE sales (
  id SERIAL PRIMARY KEY,
  employee VARCHAR(255),
  customer_and_account_no VARCHAR(255),
  product_name(255),
  sale_date(),
  sale_amount FLOAT,
  units_sold INTEGER,
  invoice_no INTEGER,
  invoice_frequency VARCHAR(255)
)

CREATE TABLE employee (
  id SERIAL PRIMARY KEY,
  customer_and_account_no VARCHAR(255),
  sale_date VARCHAR(255),
  product_name VARCHAR(255),
  sale_date VARCHAR(255),
  sale_amount FLOAT,
  units_sold INTEGER,
  invoice_no INTEGER,
  invoice_frequency VARCHAR(255)
)
